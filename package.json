{
  "name": "tg-clone-final-apk",
  "version": "1.0.1",
  "license": "MIT",
  "scripts": {
    "eas-build-pre-install": "rm -f yarn.lock && yarn install --no-frozen-lockfile",
    "build": "run typecheck && -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "run typecheck:libs && -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "expo": "^55.0.8"
  },
  "main": "expo-router/entry"
}